#lang racket

(define (read-slabs input-file)
  (call-with-input-file input-file 
    (lambda (port)
      (for/fold ([bricks '()]
                 [bricks-by-z (hash)]
                 [maxes '(0 0 0)])

        )))
